#include <iostream>
#include <vector>
#include <set>
#include <map>
#include <algorithm>
using namespace std;

int main()
{
    // add your code here
    // cout<<INT_MAX<<endl<<INT_MIN;
    set<int> s;
    multiset<int> si;
    map<int,int> m;
    sort(s.begin(),s.end());
    s.find(2);
    system("pause");
    return 0;
}
